"use strict";

// let calculateBill = (billAmount, taxRate) => {
//   console.log("Running Calculate Bill!");
//   const total = billAmount * (1 + taxRate);
//   return total;
// };

// const myTotal = calculateBill(100, 0.13);
// console.log(myTotal);

// let doctorize = (name) => {
//   return `Dr. ${name}`;
// };
// let yell = (name) => {
//   return `HEY ${name.toUpperCase()}`;
// };
